
from building import *
import rtconfig

cwd     = GetCurrentDir()

src = []

src     += []
CPPPATH = [cwd]
LOCAL_CCFLAGS = ''

if rtconfig.PLATFORM == 'gcc':
    LOCAL_CCFLAGS += ' -std=c99'
elif rtconfig.PLATFORM == 'armcc':
    LOCAL_CCFLAGS += ' --c99'
elif rtconfig.PLATFORM == 'armclang':
    LOCAL_CCFLAGS += ' -std=c99'

if GetDepend('PKG_ZMOD4410_USING_SENSOR_V1'):
    src += ["zmod4410_sensor_v1.c"]



group = DefineGroup('zmod4410', src, depend = ['PKG_ZMOD4410_USING_SENSOR_V1'], CPPPATH = CPPPATH, LOCAL_CCFLAGS = LOCAL_CCFLAGS)

Return('group')
